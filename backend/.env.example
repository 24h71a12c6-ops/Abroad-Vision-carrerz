PORT=5000

# CORS / Frontend origin allowed by backend/server.js
# If you open the frontend via VS Code Live Server, 5500 is common.
FRONTEND_URL=http://127.0.0.1:5500

# MySQL Connection
DB_HOST=127.0.0.1
DB_USER=root
DB_PASSWORD=
DB_NAME=abroad_vision_carrerz
DB_PORT=3306

# Set to true/1/required if your MySQL provider requires SSL.
# (Common on managed DBs; local MySQL usually doesn't need it.)
DB_SSL=false

# WhatsApp Cloud API (Meta)
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_ACCESS_TOKEN=
WHATSAPP_BUSINESS_ACCOUNT_ID=

# Comma-separated admin WhatsApp numbers. (Digits only preferred; code normalizes)
ADMIN_WHATSAPPS=919876543210

# Backward-compat (older key; not used by current code)
# ADMIN_WHATSAPP=+919876543210

# Email Configuration (Gmail)
EMAIL_USER=
EMAIL_PASSWORD=

# Comma-separated admin emails
ADMIN_EMAILS=

# Backward-compat (older key; not used by current code)
# ADMIN_EMAIL=admin@abroadvisioncarrerz.com

# Password reset code hashing pepper (optional but recommended)
RESET_PASSWORD_PEPPER=
